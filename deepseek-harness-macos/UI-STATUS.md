# macOS 原生界面完成度

| 界面 / 功能 | 当前状态 | 实现方式 | 说明 |
| --- | --- | --- | --- |
| 应用主窗口 | 已完成 | SwiftUI `WindowGroup` + AppKit 生命周期修复 | 检测旧窗口恢复造成的越界或过小窗口并自动居中恢复 |
| 主布局 | 已完成 | SwiftUI 原生 `HSplitView` | 左侧品牌与会话树位于同一个可整体折叠的 sidebar surface，不再由分栏框架插入额外标题安全区或横向断层；中间聊天与条件式工具详情使用嵌套 `HSplitView`，详情只在 Inspect 后出现。聊天列按 WebUI 固定 748px 上限、16px flow gap 和窄窗 32px transcript clearance。 |
| 左侧原生材质 | 已完成 | `NSVisualEffectView.sidebar` | 整个侧栏在所有系统版本都使用系统 sidebar 色/材质；Liquid Glass 仅由指定 Popover 显式使用，不再覆盖侧栏。 |
| 双层标题栏与会话导航 | 已完成（核心视觉/行为） | 全尺寸原生标题栏 + SwiftUI Canvas | 按 Codex 布局拆为两层：第一行避让系统交通灯，只放仓库 PanelLeft、真实会话后退与前进；第二行的品牌是侧边栏本身的首行，与工作区树共用一整块系统 sidebar material 并整体折叠，detail 同高度显示当前会话标题、模式和 Session log。后退/前进采用浏览器式访问栈，普通会话选择会截断 forward 分支，归档或已移除的历史项自动跳过。`DeepSeekBrandWordmark` 直接复制 `BrandWordmark.tsx` 的 17 条字形/whale/badge path 与 182×24 几何，未使用字体拼字或 WebKit。 |
| 新建会话 | 已完成（Workspace 复用语义） | `workspace.list` + `session.create` | 新建优先当前会话所属 Workspace、再取最近 Workspace；严格按 workspace membership 复用该 Workspace 已有未归档空白会话，无可复用项才创建，避免连续点击制造多个空会话。 |
| 会话列表 | 已完成（WebUI 核心行行为） | SwiftUI 原生树 | 从真实 `session.list` 读取；工作区行严格 34px，默认文件夹、hover 换仓库三角并出现省略号/加号；会话严格单行 32px、14px 标题，普通态更新时间、hover 换省略号，移除旧版常驻路径/snippet/消息图标；pending/running/subagent 使用与工具一致的 WebUI StateDot。 |
| 会话搜索 | 已完成（WebUI 核心行为/几何） | SwiftUI 折叠搜索 + 独立结果树 + `session.search` | 搜索与“工作区”及两个动作同处 36px header：静止时仅 28px 仓库 Search 图标，点击后动作区和标题淡出、展开成 30px/10px 圆角胶囊并自动聚焦，Close/Escape 清空折叠；250ms 防抖调用 Host 全文搜索；输入查询后切换为独立扁平结果树，本地标题/工作区命中按最近更新优先，Host 内容命中保持排名并与本地结果去重合并；标题、工作区、snippet 分层呈现，过滤空会话/归档会话，遵循 20 条协议上限并消费本地溢出或 Host `hasMore` 提示缩小范围；500 UTF-16 单元和 NUL 清理沿用协议约束。WebUI 本身也不做逐字命中高亮或分页加载。 |
| 会话选择 | 已完成（含层级导航） | SwiftUI | 拉取真实历史与模型目录；会话 header 按 `parentSessionId` 生成 WebUI 风格 ancestry breadcrumb，可点击父级返回 |
| 会话重命名 | 已完成 | SwiftUI Sheet | 走真实 `session.rename` API |
| 空会话 Hero | 已完成（核心视觉/Workspace 行为） | SwiftUI | 对齐“探索未至之境 / 预览版—Workspace/Agent 模式—输入框”的居中结构；Workspace 选择器只在空白新会话出现，默认回显当前 Workspace，可在首条消息前切换到目标 Workspace 的可复用空白会话。 |
| 聊天消息流 | 已完成（核心行为/节奏校准） | SwiftUI LazyVStack | 已按 assistant `message.content` 保留 reasoning/text/tool-call 顺序；实时消费 `assistant/chunk` 并按 block index 合并，final 稳定替换 partial；消费 `tool/code-dispatch-start` / `tool/code-dispatch`，按 parentCallId 递归呈现嵌套工具，含循环与 256 层上限保护。用户图片与文字合并为同一个 stack，动作栏位于整个 stack 下方；assistant 连续多个 text block 仅在已收到 `turn/end` 的 turn tail 生成一次动作栏。Agent 动作按钮保持 Copy/Like/Dislike/note/Branch 居左，完成时间与该轮用时、首 token、解码 tok/s 整组居右并仅在 hover 时出现；时间取 closing assistant，指标按 WebUI 的 turn/step timing 口径折叠。`command/run`/`command/done` 按 commandId 合并成紧凑命令行，权限切换显示 `permission · preset read-only`，不再伪装成 Agent 长回答。图标均复制仓库 SVG path，分支仅最新空闲 chat tail 可用。 |
| Markdown | 部分完成（原生块级主链路） | SwiftUI `NativeMarkdownView` + 原生 block parser + `AttributedString` inline | assistant 正文已不再压成单个 `Text`：按 WebUI 的 16/28 正文节奏、16px 段间距、h1–h4 字号/字重/32px 标题上距，分别渲染段落、ATX/Setext 标题、有序/无序/任务与混合缩进列表、continuation prose、2px 引用线、分隔线、fenced code 和横向滚动表格。代码块为 12px 圆角、9×14 banner、16px body、13/22 monospace，复用原生语法着色，复制按钮使用仓库 Copy/Check path 并在成功后切换 1 秒；streaming fence 不提前着色，settled 后按 language 自愈，行内 code 已使用 WebUI 明暗 token 和 14px monospace。现有 6 组 parser fixture 覆盖 10 种结构、Setext/分隔线优先级和双向混合嵌套列表。当前仍缺完整 CommonMark 松散列表/多块列表项、远程图片、脚注、KaTeX，以及行内 code 的 5px 内边距/6px 圆角逐像素能力，因此不宣称与 WebUI mdast renderer 完全等价。 |
| 思考行 | 已完成（核心行为/截图再校准） | 自定义 SwiftUI disclosure | 24px、默认折叠、Think 与 disclosure chevron 均使用仓库精确资源路径、hover 时图标预览切换 chevron、点分隔、无大卡背景；运行中显示最新行并跟随增量内容，按 WebUI 2.6s ease-out 的 90% 移动/10% 停顿 sweep；展开后移除行内摘要，只显示 22px 缩进、tertiary 色和 14/24 节奏的完整原文；完成后切回首行摘要，retry 会撤回失败尝试的 partial，并尊重 Reduce Motion。 |
| 工具行 | 已完成（核心行为/源码与真实历史再校准） | 自定义 SwiftUI disclosure + Host opener | 默认折叠纯文本状态行，只显示工具正在做什么；点击后才展开正文。严格按正文、非空输出或专用卡片判定是否可展开，无正文工具不显示 chevron；标题在 running/ok/error/stopped 始终稳定为 Bash/Read/Search/Write/Edit/Code/Tool call 或 Host 专属标题，生命周期仅由 sweep、状态点和错误摘要表达；运行态与 Think 使用同源 2.6s sweep；hover 图标切仓库原版向下 chevron，错误/中断替换为 WebUI 状态点。除 Bash/Read/Edit/Write/Grep/Glob/Web Search/Fetch 外，已补 WebUI keyed 行：`todo_write` 的完成计数、进行中任务与独立 `+n`，`ask_user_question` 的等待/取消/中断/已回答计数，`skill` 名称。Cordis 已改为真正独立 presenter：define 为 24px business-accent disclosure；run/update/stop/remove 为 32px 行；chevron/stop/trash 使用仓库精确 SVG path；工具结果保留 `data.meta` 中 Host 铸造的 plugin/package/run ID，而非从输出猜测。工具结果消费 `error.code/name`，空正文回退错误名与错误码，普通 JSON 参数 pretty-print；文件链接只限 read/write/edit。当前真实 Host 的 120 条 history 仍主要覆盖 `run_code` 与 `CODE_RUN_FAILED`；todo/ask/skill 的真实事件样本仍待验收，Cordis 协议以仓库 executable snapshot 与夹具校验。 |
| 工具展开正文 | 部分完成 | SwiftUI 原生滚动卡片 | 通用 IN/OUT 以及 Terminal、Diff、Read、Search、Web 五类 Host render-intent 卡已实现；五类卡分别按 WebUI primitive 保留独立结构，统一 12px 圆角/code-block surface 而不再套第二层大卡，Read/Search banner 使用 9×14 与独立 banner token，Diff body/footer 使用 12×14 / 0×14×12，Search body 使用 8×14×12×0，Web 使用 12×14 与 320px sources cap，并保留 ToolRow 的 4px 展开间距。聊天流的 Terminal 与 WebUI 一致不截断中段并保留 224px 输出滚动上限，运行时只显示 banner，状态点用精确 DeepSeek-450/green-500/amber-500/red token 和 1s 八像素追逐；Terminal/Read/Search/Diff 的内容行强制 pre 语义、不软换行、横向滚动。详情使用默认 16 行；Diff/Read/Search 使用 8/16 行首尾折叠，统一采用 `ceil(maxLines/2)` 的 head。Search 已补空结果/隐藏复制、将截断 recovery 移到卡外，以及 tail 从某文件匹配中段开始时恢复归属 header、消费一个 tail slot并保持可见行数不超 cap。Terminal 已加入原生 ANSI/终端重放：16/256/truecolor、背景、粗斜体/下划线/删除线、OSC/C0 清理、CR/退格/CSI K、tab stop、组合字符与常见宽字符；仅视觉为空时显示“无输出”并隐藏复制，复制仍保留原始 output。Code 与 Read 已接入纯原生 `AttributedString` 语法着色：沿用 WebUI 固定 alias allowlist、整窗扫描跨行注释/字符串、未知语言纯文本回退，并使用仓库 `shiki.css` 的精确亮/暗色 token；它是原生状态机 lexer，复杂 TextMate 嵌入语法仍不宣称与 Shiki token 边界完全相同。Skill 使用独立“说明”卡。Cordis define 已实现默认 Client、Host/Client 缺失禁用、32px tabs、JavaScript 着色、260px source/output、结果区、panel hint 与 Inspect；run 卡读取真实 `dynamicCordisRunner/inventory`，呈现待激活/待审批/失败/Client 待激活/运行中/已移除/已有更新，并由本会话成功 undefine 覆盖 wire。原生端不执行模型写入的 React Client half，所以不会伪造“运行中”，也无法呈现 Package-owned business child view；全局面板的清单、版本、停止与移除已实现，审批/运行仍受这一原生执行边界限制。当前 Mac 锁屏阻止最新版本逐像素截图验收。 |
| Cordis 全局面板 | 部分完成（真实 inventory/安全动作） | SwiftUI 原生 420px Popover + `dynamicCordisRunner/*` Remote | 左下设置上方使用仓库 `IconCordisPluginOutline14` 精确 path，49px 入口、running/approval 计数、待审批自动打开；面板按当前/其他会话分组并将待审批置顶，支持版本选择、状态 pill、current/next transition、Host failure 与动作错误。停止和移除使用真实 `stopFromPanel` / `undefineFromPanel`，参数经 Host descriptor 探针确认是 `agentId/pluginId`，并刷新权威清单。原生 AppKit 不执行不受信任的 React Client half，因此审批/运行/重试/回退目前只显示诚实提示，不伪造完成；完整审批与 Package business child view 仍需原生插件 ABI 或受支持的非 WebKit Client runtime。 |
| 工具详情栏 | 已完成（结构与会话隔离） | SwiftUI 条件式 DetailsPanel | 默认完全隐藏，Inspect 后才在聊天右侧打开；整行点击只展开/折叠；header 显示真实 wire tool name，Input 显示完整 pretty JSON，Output 复用 Terminal/Diff/Read/Search/Web presenter；切换、归档或新建/打开会话会清空旧会话详情。 |
| 输入框 | 已完成（源码尺寸校准） | SwiftUI 多行 `TextField` | 按 WebUI card 重排为 780px max、22px radius、10px top、12px textarea/toolbar gap、输入 16/12/4 padding、toolbar 2/8/6；自动增长上限由 8 行改为 14 行，Enter 提交。左侧命令入口从近似 `slash.circle` 改为仓库 `IconPlusOutline16` 精确 path、28px selector 圆形 seat，并按 tools 16px、trailing 12px 分组。待 Mac 解锁后做窗口像素验收。 |
| 发送与停止 | 已完成（仓库图标/尺寸校准） | SwiftUI + 真实 RPC | `session.prompt` / `session.cancel`；发送按钮由近似 SF Symbol 改为仓库 `IconSendOutline16` 精确 path，34px DeepSeek 500/400 动态蓝底、静态白箭头、空输入 opacity 0.4、向上光学校准 2px；停止态使用 WebUI 当前内联 10×10、3px 圆角白色方块。 |
| 模型选择 | 已完成（WebUI 两级交互） | SwiftUI 原生 Liquid Glass Popover + `session.models/session.selectModel` | 28px 模型名 + Effort + 旋转 chevron 触发器；根层严格为 Model/Effort 两行，分别进入 provider 分组模型目录和动态推理强度列表；240px 弹层在 macOS 26 使用指定 Liquid Glass、旧系统回退原生 popover material，并补 3px 蓝色键盘 focus ring；上下键循环、Escape 子层返回/根层关闭均按 WebUI 实现。每次打开重拉真实目录并提供加载失败重试；切换模型不把 provider 默认 Effort 错写成显式覆盖。 |
| 会话底部统计 | 已完成 | `sessionStats` + `tokenUsage` 投影 | Composer 下方按 WebUI 分组/省略规则显示轮数/步数、LLM 与工具时长、平均 TTFT 与 tok/s、缓存命中、输入/输出 token；使用全日志耐分页/压缩投影，不硬编码截图数字。旧 Host 缺 `sessionStats` 时只对可证明的 trajectory 计数/时长做兼容回退。 |
| 上下文用量 | 已完成（核心交互） | SwiftUI ring + 原生 Popover | 使用真实 contextPressure/contextBreakdown 投影；28px trigger/14px ring，点击显示 264px、12px padding/radius 的 WebUI 结构：标题与 `~used / window`、4px 分段条、系统提示词/工具/对话消息三行。弹层使用原生瞬态玻璃材质，不再使用系统 Menu，也不混入累计 token。最新像素截图仍受系统捕捉失败影响，暂不宣称逐像素验收完成。 |
| 权限选择 | 已完成（WebUI 状态机） | SwiftUI 原生 Popover + 窗口内风险确认 | 权限项完全来自真实 Host `permissions` 投影并过滤 `custom`，投影缺失时隐藏入口；28px 图标/名称/旋转 chevron 触发器、240px 材质弹层、勾选、hover/focus、上下键循环与 Escape 按 WebUI 实现，标准三项使用 `PermissionSelect.tsx` 精确 SVG path，Host 自定义名称和 description 原样保留。普通项经 `/permission` 乐观提交，运行中保持可选；Full access 使用 440px 遮罩卡，必须先勾选“我已了解风险，并愿意继续”，取消、关闭和会话切换均重置 acknowledgement。已用当前 Host 的 read-only/workspace-write/danger-full-access 投影验证解析，未为测试擅自切换真实权限。 |
| 消息上下文菜单 | 已完成（核心行为/反馈闭环） | 原生 contextMenu + `messageFeedback` Remote | Copy、Branch、工具输入/输出/Inspect；assistant 消息支持 Like/Dislike toggle、添加/编辑/清空 note，文案严格沿用 WebUI 的“好的回答 / 有问题的回答 / 取消标记”，已选赞踩保持仓库 outline 图标、仅切 primary 颜色；有 rating 后才显示“补充说明”，输入占位与 WebUI 一致。所有写入按 WebUI 的 version CAS 提交，切换 rating 保留已有 note，失败或版本冲突后重拉权威列表。 |
| 会话上下文菜单 | 已完成（WebUI 行为） | 原生 Menu/contextMenu + `session.fork` / `workspace.archiveSession` | 任意非空普通会话平时显示更新时间，hover 切换仓库省略号菜单，菜单及右键均严格为重命名、分叉会话、归档会话；搜索结果按 WebUI 只负责导航，不额外提供操作菜单。侧栏分叉不传 `atSeq`，由 Host 在最后完整轮次截断；归档按 workspace 全局归档集处理。 |
| 实时刷新 | 已完成 | URLSession WebSocket + SwiftUI 状态 | 监听 mux 事件并节流刷新 |
| 连接状态 | 已完成 | SwiftUI | 显示已连接、重连中、连接失败 |
| 设置主界面 | 已完成 | SwiftUI `Settings` + Sheet | 侧栏样式与网页版设置结构一致 |
| 服务器设置 | 已完成 | SwiftUI | 可修改 Host 地址并重新连接 |
| 外观设置 | 已完成 | SwiftUI | 浅色、深色、跟随系统 |
| 模型设置页 | 部分完成（真实目录/提供方读写） | SwiftUI 原生目录 + `llm.providers/settings.describe/settings.mutate/credentials.*` | 按 WebUI Models store 的三方 join 读取 37 个当前真实可配置提供方、namespace profile、revision/applies、凭证 configured/source/writable；默认只列已启用或已配置项，可搜索和展开全部可添加提供方。未配置 route 现在可通过 SecureField 添加：先用 revision-fenced `settings.mutate` 写入派生的 `<ROUTE>_API_KEY` profile 引用，再写 `credentials.set`；已配置项可更新密钥，可移除 user-only profile，并只清理由本页派生且可写的凭证。密钥从不读取或回显。Provider 的 baseURL/model catalog/schema 表单、手工声明 route 与 discoverModels 仍待接入，因此仍标为部分完成。 |
| 插件设置页 | 部分完成（真实配置卡 + 安装库存） | SwiftUI 原生分段页 + `settings.describe/mutate`、`credentials.*`、`pluginInventory/list` | 页面分为“可配置 / 已安装”：可配置页接入 WebUI 同三张 Bash、Agent Loop、Web Search 卡，读取 effective/user/base/revision，支持数值/地址字段 set 与清空后 unset，保存使用 revision-fenced `settings.mutate`；Web Search 密钥按 effective `apiKeyEnv`（默认 `DEEPSEEK_API_KEY`）写入凭证域并只显示 configured 状态。已安装页保留真实 Loader 库存、搜索、启用状态与 Cordis phase。字段级 Host schema 错误目前通过全局错误提示呈现，尚未实现 WebUI staged form 的逐字段错误与冲突重载细节。 |
| Agent 预设设置页 | 部分完成（真实 roster/管理入口） | SwiftUI 原生 TextEditor + `agentPreset.list/select/read/copy/openDocument/remove` | 读取真实 roster，空会话可选择并在新建时提交默认 preset；系统预设只读，用户预设提供原生编辑草稿、复制草稿、打开真实目录并保留 Host 写入边界；preset 创建向导和直接写入 RPC 尚未由协议提供 |
| 工作区管理 | 部分完成（真实分组/管理/拖拽核心链路） | SwiftUI 原生树 + `workspace.list/create/rename/delete/archiveSession/insertBefore/insertSessionBefore` | 按 Host 顺序分组、默认折叠、未分组桶、工作区新建/重命名/删除、按工作区创建新会话、归档会话已接入；工作区行已移除 WebUI 不存在的会话计数副标题，并按源码实现 folder→chevron 与 hover actions。工作区和组内会话支持本地乐观拖拽、按行上/下半部解析 before/after、顶部/底部蓝色插入线、Host 持久化及失败回滚。平铺/最近更新视图遵循 WebUI 投影不提供持久化手动排序；取消拖拽的系统级回调和完整复制型 hover card 仍待补。 |
| 会话轨迹页 | 部分完成（核心行为） | SwiftUI + 真实 history ledger | 已按真实历史构建 Turn → Step → Assistant/Tool/Subtool 记录，包含 32px 工具栏、三泳道概览、折叠 Turn/Calls、搜索、用量/时长列与本地 Inspector；用当前会话验证为 5 Turns、99 Steps、352 条记录。尚缺网页版 request header/prompt diff、多标签详情、时间线拖拽选区、历史分页和百行以上虚拟化 |
| 审批面板 | 已完成（核心行为） | 原生 composer takeover + `/api/respond` | 消费可重放 `approval/requested`，显示等待条、原因和配对命令；支持拒绝/允许一次、one-shot busy 和 resolved-frame 移除 |
| 用户问题面板 | 已完成（核心行为） | 原生 SwiftUI 分步表单 + `/api/respond` | 单选自动前进、多选、自由输入、推荐标签、详情、上一题/下一题、逐题跳过、整批结构化答案与取消均按协议实现 |
| 计划审阅 | 已完成（核心行为） | intent-aware 原生卡片 | 仅在单问题、二元选项、合法 `plan-review` intent 时接管；Markdown 计划、批准/拒绝按原 option label 回传，未知/不完整 intent 回退通用问题表单 |
| Todo / Goal 条 | 已完成（核心行为） | 原生 SwiftUI composer dock + Host projection | 从 history tail `projections` 和实时 `session/projection` 高水位更新；Todo 默认折叠、状态计数和列表；Goal 阶段、暂停/继续、CAS 编辑、清除与 blocked 提示均接真实 RPC |
| 队列与 Steering | 已完成（核心行为） | 原生 SwiftUI queue dock + `session/queue` | 权威全量快照；单条直显、多条默认折叠；编辑、移除、逐条 steer 走真实 `session.updateQueue`；pending steering 在对话末尾用半透明用户气泡呈现 |
| 附件与图片 | 已完成（WebUI 核心行为） | SwiftUI rail / wrapping `Layout` + AppKit material overlays + `session.prompt` image parts + `session.attachment` | 支持 PNG/JPEG/WebP/GIF 文件拖放、草稿缩略图、移除和顺序化 canonical base64 上传；真实消费 history 的 `imageLimits` 投影，以 WebUI 同文案在加入前检查媒体类型、数量、单图字节、总字节与像素数，DragMask 同步显示 Host 数量/单图上限及允许/阻止状态。历史连续图片合并为图库，单图严格把比例夹到 0.25～4、长边 240px 且不放大，多图 64px tile、10px gap 并原生换行。历史图与草稿图共用全窗口原生 lightbox：40px 安全边距、12px 圆角、右上 36px 仓库 Close 图标，Escape/遮罩/按钮关闭并恢复焦点。DragMask 覆盖全三栏，使用 WebUI 同色遮罩；允许态和禁用态均直接复制仓库 SVG 路径与配色，禁用态为灰卡 + 橙色禁止徽章。通用 RPC 错误保留 `attachment-error.details.reason`，发送失败时按 WebUI 顺序映射模型、子智能体、像素、格式、数量与大小文案，未知 reason 使用带码兜底且保留草稿图。 |
| 产出文件 | 部分完成（核心行为） | SwiftUI 自适应 chips + `NSWorkspace` | 按 WebUI turn-tail 规则，仅把成功 mutation call view 的 `locations.path` 归入该轮末尾；最多 6 个、自适应收缩、余量计数，支持原生打开和 Finder。当前保存的真实会话没有符合条件的 mutation location，协议与空态已验证，仍待真实产出事件交互验收 |
| 子智能体导航 | 部分完成（核心导航/continuation） | SwiftUI 原生递归树 + `subagent.list/history/prompt/interrupt` | 会话 header 显示父级返回；popover 按 WebUI catalog 使用 `hasChildren` 箭头递归懒加载，展示诊断节点、mode、activity、token output、duration 和 continuable 中断；侧边栏父会话补 running descendant 状态点；打开子会话使用 `subagent.history`，continuable 使用 `subagent.prompt`，one-shot/父离线显示 WebUI 同语义只读 composer；完整 breadcrumb 与拖拽/树键盘导航仍待补 |
| 后台任务列表 | 已完成（核心行为） | `session/jobs` + SwiftUI Popover | 无任务不显示；live 优先且按开始时间排序，settled 最新优先；336px 弹层、状态点、kind、label、detail、每秒更新时长均按 WebUI 行为实现 |
| Slash 命令菜单 | 已完成（核心行为） | 原生 SwiftUI popover + Typert `commands/list` Remote | 按当前 session 拉取真实命令目录；显示名称、描述和输入 hint，本地名称/描述过滤，选择后按是否需要输入将命令插入 composer；执行继续走 Host 的 slash prompt 管线 |

状态含义：已完成 = 能连真实后端使用；部分完成 = 主链路可用但缺少网页版子功能；占位 = 有入口但尚无完整业务；未实现 = 当前没有原生入口。
